import 'package:flutter/material.dart';

// MVP 骨架页面 — 后续实现

class SalarySetupPage extends StatelessWidget {
  const SalarySetupPage({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('薪资设置')),
      body: const Center(child: Text('薪资配置页面 · 开发中')),
    );
  }
}
